<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/quizText</item>
        <item name="colorPrimaryDark">@color/quizText</item>
        <item name="colorAccent">@color/greenLogo</item>
    </style>

    <!--TabLayout Text-->
    <style name="CustomTabText" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">16sp</item>
        <item name="textAllCaps">false</item>
    </style>

    <!--Custom SeekBar-->
    <style name="SeekBarLine" parent="Theme.AppCompat.Light">
        <item name="android:colorControlActivated">@color/greenLogo</item>
        <item name="android:colorControlNormal">#ffffff</item>
    </style>

    <!--Test Toolbar-->
    <style name="TestBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColorPrimary">#ffffff</item>
        <item name="android:textColorSecondary">#ffffff</item>
        <item name="android:background">#2f3152</item>
    </style>

    <!--Menu Item Theme-->
    <style name="TestPopUpStyle" parent="Theme.AppCompat.Light.NoActionBar">

        <item name="overlapAnchor">false</item>
        <item name="android:textColorPrimary">#ffffff</item>
        <item name="android:textColorSecondary">#ffffff</item>
        <item name="android:dropDownVerticalOffset">-4dp</item>
        <item name="android:background">#2f3152</item>
    </style>

    <!--Default Action Bar-->
    <style name="AppTheme.NoActionBar">
        <item name="android:statusBarColor">@color/quizText</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!--Splash Theme-->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>


    <!--<style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>-->

    <!--<style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />-->

</resources>
